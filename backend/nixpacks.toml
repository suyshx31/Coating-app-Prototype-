# Railway (Nixpacks) build config for the backend service.
# LibreOffice is required at runtime: /work-orders/{id}/generate-report converts
# the filled NOV xlsx template to PDF via `soffice --headless`.
[phases.setup]
aptPkgs = ["libreoffice-calc"]
